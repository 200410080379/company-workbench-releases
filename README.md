# 公司工作台 · 版本下载

用于员工档案、项目成本与分成、员工账户和税前工资核对的本地工具。Windows 启动后在浏览器中使用，账本保存在自己的电脑上。

[下载最新版本](https://github.com/200410080379/company-workbench-releases/releases/latest)

## 当前版本：0.19.0

工资、公积金和实发金额自动联动，无需确认工资或登记已发放。

- **实发一直可编辑**：填写后自动保存并记入账户，修改只处理差额；6000改7000补扣1000，7000改5000返还2000。旧已发工资也可修改实发，原始快照留在操作记录中。
- **工资自动入账**：应计工资取自员工管理对应月份；建立工资表后自动计入账户。前月实发变化会更新后续月份余额。
- **公积金自动扣账**：员工管理金额作为标准，员工账户手工“公积金/公积金补缴”作为额外补缴，独立累加。例如标准9元加补缴1000元，合计1009元。
- **金额支持千位分隔符**：如 `10,000`、`10，000`、`10 000`、`10’000`，不猜测错误分组或小数逗号，不处理工号等文本。
- 保留“重新开始核算”：预览和备份后，用户确认才清空项目并作废旧工资表；保留员工与个人收支。

**升级后的记账变化**：有效工资表中已填写的实发（包括默认按基础工资填写的金额）会按新规则同步入账，已入账部分不重扣。标准公积金自动处理本月及已有可计算的未发工资月份；旧手工公积金视为额外补缴保留。升级前自动备份，不会自动清空项目，也不执行银行转账。

**已知限制**：Windows 更新器未改动，此前反馈的 `EPERM` 目录改名失败与 GitHub 连接超时处理仍未修复；遇到更新问题请保留原程序目录和 `data/backups`。

### 旧客户升级，保留原有数据

- **0.15.1及以上用户**：在软件内点击“版本与更新 → 检查更新”，下载安装即可，无需登录GitHub。
- **0.15.0 用户**：在原程序“版本与更新”中选择本次发布的 `staff-workbench-0.19.0.swupdate`，确认其他页面已保存后安装。此次升级会接入在线更新地址。
- **更早版本用户**：下载完整 Windows ZIP，解压到一个新文件夹；保存旧网页修改并在旧服务窗口按回车退出。运行新包中的“升级已有客户.cmd”，选择原程序目录。更新完成后仍从原目录启动。
- **以后更新**：在“版本与更新”中点击“检查更新”，下载并安装新版；也可使用离线更新包。

不要直接用新文件夹覆盖旧程序的 `data`，也不要为了更新删除原账本。

### 新客户首次使用

下载完整 Windows ZIP，完整解压后双击“公司工作台.exe”。无需另外安装 Node.js。通用发布包不包含任何真实员工、工资或项目数据，首次启动建立空账本。

## 数据与备份

账本在程序旁的 `data` 文件夹。更新只下载和替换程序，不上传客户资料。更新前自动建立 SQLite 备份，并用副本核对数据迁移；升级失败或新版首次启动失败时恢复更新前程序和账本。

备份在 `data/backups/before-update-*`。迁移电脑时先正常退出，再完整复制原程序文件夹。

## 发布附件

| 文件 | 用途 |
| --- | --- |
| `company-workbench-windows-0.19.0.zip` | 完整 Windows 版；首次使用或早期版本接入更新器 |
| `staff-workbench-0.19.0.swupdate` | 已接入更新器的客户使用的离线更新包 |
| `latest.json` | 软件自动读取的已签名版本信息，无需手动打开 |
| `SHA256SUMS.txt` | 发布附件的 SHA-256 校验值 |

GitHub 自动提供的“Source code”压缩包只包含这个发布仓库的说明，**不是可运行程序**；请下载上表中的 Windows ZIP。

## 验收范围

支持 Windows 10/11 x64。已完成程序测试、旧版账目迁移测试、签名和文件校验、失败恢复测试。Windows 双击升级流程仍需实机验收，EXE 暂未做 Windows 代码签名；更新包使用独立发布签名验证来源。

本工具计算税前数据，不执行银行转账。业务计算与最终工资请按公司实际情况核对。

本仓库用于发布通用软件和版本说明，不存放客户员工档案、工资数据库、备份或发布私钥。
